/**
 * Manual seeding algorithm - keeps teams in the order they were input
 * @param {Array} teams - Array of team objects
 * @param {Object} options - Options for seeding
 * @returns {Array} - Array of seeded teams
 */
export function manualSeeding(teams, options = {}) {
    return [...teams]
  }
  